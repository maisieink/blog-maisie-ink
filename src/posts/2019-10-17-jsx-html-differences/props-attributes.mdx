##### ℹ️ _attributes_ and _props_ are analogous:

- HTML elements have _attributes_.
  - e.g., `maxlength` in `<input maxlength="16" />`
- JSX elements have _props_.
  - e.g., `maxLength` in `<input maxLength="16" />`
